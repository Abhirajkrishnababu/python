# Simple if
# Question:
# Check if a given year is a leap year. Print "Leap year" if it is, otherwise print "Not a leap year".
# (A leap year is divisible by 4, but not divisible by 100 unless it is divisible by 400.)

# Answer:
year_leap_check = int(input("Enter a year: "))
if (year_leap_check % 4 == 0 and year_leap_check % 100 != 0) or (year_leap_check % 400 == 0):
    print("Leap year")
else:
    print("Not a leap year")
