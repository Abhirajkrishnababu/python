# Simple if
# Question:
# Determine the type of a given triangle based on its angles.
# Print "Acute-angled", "Right-angled", or "Obtuse-angled" accordingly.

# Answer:
angle1 = float(input("Enter the measure of angle 1: "))
angle2 = float(input("Enter the measure of angle 2: "))
angle3 = float(input("Enter the measure of angle 3: "))

if angle1 + angle2 + angle3 == 180:
    if angle1 < 90 and angle2 < 90 and angle3 < 90:
        print("Acute-angled")
    elif angle1 == 90 or angle2 == 90 or angle3 == 90:
        print("Right-angled")
    else:
        print("Obtuse-angled")
else:
    print("Invalid triangle")
