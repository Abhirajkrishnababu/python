# Variables: Finding Factors
# Question:
# Find and print the factors of a given number. Take the number as input from the user.

# Answer:
def find_factors(number):
    factors = [i for i in range(1, number + 1) if number % i == 0]
    return factors

num_to_factor = int(input("Enter a number to find its factors: "))
result_factors = find_factors(num_to_factor)
print("Factors of", num_to_factor, "are:", result_factors)
